export enum USER_ROLE_TYPES {
    PROVIDER = 'provider',
    CUSTOMER = 'customer',
    ADMIN = 'admin'
}

export enum VERIFICATION_TOKEN_TYPE {
    REGISTER = 'register',
    LOGIN = 'login'
}

export enum LOCALE_TYPE {
    ENGLISH = 'en',
    HINDI = 'hi'
}

export enum STATUS {
    ACTIVE = 'active',
    IN_ACTIVE = 'in_active'
}

export enum SERVICE_REQUEST_STATUS {
    PENDING = 'pending',
    ACCEPTED = 'accepted',
    REJECTED = 'rejected',
    COMPLETED = 'completed'
}

export enum PROPOSAL_ENUM {
    PENDING = 'pending',
    ACCEPTED = 'accepted',
    REJECTED = 'rejected',
    VIEWED = 'viewed'
}
